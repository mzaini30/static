{
  "name": "coba-sergey",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate:sergey": "sergey-zen",
    "generate:windi": "windicss public/**/*.html -mts -f windi.config.js",
    "dev:nodemon": "nodemon -e html --ignore public -x run-s generate:*",
    "dev:liteserver": "lite-server",
    "dev": "run-p dev:*",
    "build:hapus": "del-cli public/",
    "build": "run-s build:hapus generate:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^3.0.1",
    "lite-server": "^2.6.1",
    "nodemon": "^2.0.7",
    "npm-run-all": "^4.1.5",
    "sergey-zen": "^0.0.16",
    "windicss": "^2.5.14"
  }
}
